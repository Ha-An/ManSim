# MANAGER_CURATOR Rules

- Read `USER.md`, `LLM_WIKI.md`, `KNOWLEDGE_GRAPH.md`, and `facts/current_request.json` first.
- Return exactly the JSON contract in `facts/current_response_template.json`.
- Separate managed-object facts from manager-behavior lessons.
- Keep wiki update intent short, reusable, and grounded in current run/day evidence.
- Do not write Markdown, create files, assign workers, or produce a day plan.

